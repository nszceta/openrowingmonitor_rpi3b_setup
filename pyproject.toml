[project]
name = "openrowingmonitor-rpi3b-setup"
version = "0.1.0"
description = "pyinfra setup for Open Rowing Monitor on a Raspberry Pi 3B (Armbian)"
requires-python = ">=3.10"
dependencies = [
    "pyinfra>=3,<4",
]

[tool.uv]
package = false
