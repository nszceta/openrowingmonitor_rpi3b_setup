[project]
name = "openrowingmonitor-sbc-setup"
version = "0.1.0"
description = "pyinfra setup for Open Rowing Monitor on an SBC (Armbian or a Debian/Ubuntu-based equivalent)"
requires-python = ">=3.10"
dependencies = [
    "pyinfra>=3,<4",
]

[tool.uv]
package = false
